App.Network.Neuron = function(x, y) {
    var x = x;
    var y = y;
    var wx = Math.random();
    var wy = Math.random();

    return {
        x: x,
        y: y,
        wx: wx,
        wy: wy
    }
}

